import { ArticleLayout } from '@/components/ArticleLayout';
import reskin from './reskin.png';

export const article = {
  author: 'Gavin Woods',
  date: '2024-09-25',
  title: 'New Website Overhaul',
  description:
    'New website overhaul. After 3 years of telling myself I would reskin my portfolio website I finally got down to doing it!',
};

export const metadata = {
  title: article.title,
  description: article.description,
};

export default (props) => <ArticleLayout article={article} {...props} />;

After 3 years I finally rebuilt my website using Next.js and Tailwind CSS.

<Image src={reskin} alt="" />